File getProjectDir(String dir) {
    return new File("${buildscript.sourceFile.getParent()}/${dir}")
}

if (getProjectDir('../core/iot-channel/settings_api.gradle').exists())
    apply from: getProjectDir('../core/iot-channel/settings_api.gradle').absolutePath