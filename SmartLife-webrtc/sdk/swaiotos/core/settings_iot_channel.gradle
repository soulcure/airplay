File getProjectDir(String dir) {
    return new File("${buildscript.sourceFile.getParent()}/${dir}")
}

if (getProjectDir('iot-channel/settings_core.gradle').exists())
    apply from: getProjectDir('iot-channel/settings_core.gradle').absolutePath