File getProjectDir(String dir) {
    return new File("${buildscript.sourceFile.getParent()}/${dir}")
}

if (getProjectDir('runtime/settings_core.gradle').exists())
    apply from: getProjectDir('runtime/settings_core.gradle').absolutePath